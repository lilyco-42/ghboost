# W8 注入三内核后 packageRelease 要压缩 ~300MB native 库 × 4 个 split 输出
#（universal + 3 ABI），2G 堆下两个并行 IncrementalSplitterRunnable 同时炸
#（典型 OOM 形态）—— 升到 4G（runner 16GB 富余）。
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
